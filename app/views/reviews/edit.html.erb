<h2 class="mt-5">Edit Review for <%= Destination.find(session[:destination_id]).name.titlecase %></h2>
<%= render :partial => 'form' %>
  <div class="col">
    <%= button_to 'Delete Review', @review, :method => 'delete', :class => 'btn btn-danger w-100', :data => {:confirm => "Are you sure you want to delete this review?"}   %>
  <div class="col">
</div>